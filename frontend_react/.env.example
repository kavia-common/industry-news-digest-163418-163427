# API base URL for backend REST endpoints
# The deployment pipeline will map this to the proper environment.
REACT_APP_API_BASE=https://api.example.com
# Public site URL used by email flows if needed (for redirect links)
REACT_APP_SITE_URL=https://news.example.com
